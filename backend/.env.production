# Backend Production Environment (Render)
# Update these values before deploying to Render

# MongoDB Atlas Connection String (get from MongoDB Atlas dashboard)
# Format: mongodb+srv://username:password@cluster.mongodb.net/society-tracker
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/society-tracker

# Server Port (Render sets this automatically to 3000, but this is fallback)
PORT=3000

# Frontend URL for CORS (update after frontend is deployed to Render)
# Replace with your actual Render frontend URL
# Example: https://society-tracker-frontend.onrender.com
CORS_ORIGIN=https://your-frontend-name.onrender.com

# Environment
NODE_ENV=production
