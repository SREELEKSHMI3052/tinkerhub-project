# Backend Production Environment (Render)
# Replace these values with your actual deployment URLs and credentials

# MongoDB Atlas Connection String
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/society-tracker

# Server Port (Render will set this automatically, but this is the fallback)
PORT=3000

# Frontend URL for CORS (replace with your Render frontend URL)
CORS_ORIGIN=https://your-frontend-name.onrender.com

# Environment
NODE_ENV=production
